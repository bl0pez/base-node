{
  "name": "03-typescript",
  "version": "1.0.0",
  "description": "1. Clonar el repositorio.\r 2. Ejecutar el comando `npm install` para instalar las dependencias.\r 3. Ejecutar el comando `npm run dev` para ejecutar el proyecto.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon",
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && node dist/app.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.10.2",
    "nodemon": "^3.0.2",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.2"
  }
}
